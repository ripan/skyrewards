<!DOCTYPE html>
<html>
	<head>
		<title>SkyRewards</title>
		<%= stylesheet_link_tag    'application', media: 'all' %>
		<%= javascript_include_tag 'application' %>
		<%= csrf_meta_tags %>
	</head>
	<body>
		<%= render :partial => 'shared/header'%>
		<div class="container">
			<div class="row">
				<%= yield %>
			</div>
		</div>
		<%#= render :partial => 'shared/footer'%>
	</body>
</html>